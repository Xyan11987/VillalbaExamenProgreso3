<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VillalbaExamenProgreso3.Views.NewPage1"
             Title="Diego Villalba">
    <ContentPage.BindingContext>
        <vm:SearchViewModel />
    </ContentPage.BindingContext>
    <StackLayout Padding="10">
        <Entry x:Name="DVillalba_entryCountry" Placeholder="Ingrese el nombre del país" Text="{Binding CountryName}" />
        <Button x:Name="DVillalba_buttonSearch" Text="Buscar" Command="{Binding SearchCommand}" />
        <Button x:Name="DVillalba_buttonClear" Text="Limpiar" Command="{Binding ClearCommand}" />
    </StackLayout>
</ContentPage>

